# Índice de specs

Estados: `borrador` → `en clarificación` → `aprobada` → `planificada` → `en implementación` → `implementada`.
"Marcadores" = `[NECESITA ACLARACIÓN]` abiertos; deben llegar a 0 antes de `aprobada`.
Las skills actualizan esta tabla; si la editas a mano, mantén el recuento real (`grep -c`).

| # | Spec | Estado | Depende de | RF | Marcadores | Notas |
|---|---|---|---|---|---|---|
| 001 | [{{nombre}}](001-{{nombre}}/spec.md) | borrador | - | 0 | 0 | |

## Preguntas consolidadas para {{cliente}}
1. 
